# Server-only secrets. Configure real values in Vercel; never commit them.
DATABASE_URL=
DIRECT_URL=
CRON_SECRET=
OCM_API_KEY=
# Backward-compatible alternative: OPEN_CHARGE_MAP_API_KEY=

# Existing PlugV email reminder service.
ZOHO_SMTP_USER=
ZOHO_SMTP_PASSWORD=
