import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, ArrowRight, ExternalLink } from "lucide-react";
import SiteHeader from "@/components/home/SiteHeader";
import SiteFooter from "@/components/home/SiteFooter";
import CostCalculator from "@/components/knowledge/CostCalculator";
import CategoryLists from "@/components/knowledge/CategoryLists";
import VehicleRankingList from "@/components/knowledge/VehicleRankingList";
import RelatedGuides from "@/components/knowledge/RelatedGuides";
import { getKnowledgeArticle, knowledgeArticles } from "@/data/knowledge-articles";
import { absoluteUrl, safeJsonLd, SITE_URL } from "@/lib/seo";

type Props = { params: Promise<{ slug: string }> };
export function generateStaticParams() { return knowledgeArticles.map(({ slug }) => ({ slug })); }
export async function generateMetadata({ params }: Props): Promise<Metadata> { const article = getKnowledgeArticle((await params).slug); if (!article) return {}; return { title: article.title, description: article.description, keywords: article.targetKeyword ? [article.targetKeyword, "electric cars India", "PlugV"] : undefined, alternates: { canonical: `/knowledge/${article.slug}` }, openGraph: { type: "article", title: article.title, description: article.description, url: `/knowledge/${article.slug}`, publishedTime: article.updatedAt, modifiedTime: article.updatedAt, images: [{ url: "/brand/plugv-social-card.png", width: 1200, height: 630 }] } }; }

export default async function KnowledgeArticlePage({ params }: Props) {
  const article = getKnowledgeArticle((await params).slug); if (!article) notFound();
  const schema = { "@context": "https://schema.org", "@graph": [{ "@type": "Article", headline: article.title, description: article.description, dateModified: article.updatedAt, datePublished: article.updatedAt, mainEntityOfPage: absoluteUrl(`/knowledge/${article.slug}`), author: { "@type": "Organization", "@id": `${SITE_URL}/#organization` }, publisher: { "@id": `${SITE_URL}/#organization` } }, { "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: "Home", item: SITE_URL }, { "@type": "ListItem", position: 2, name: "Knowledge Hub", item: absoluteUrl("/knowledge") }, { "@type": "ListItem", position: 3, name: article.shortTitle, item: absoluteUrl(`/knowledge/${article.slug}`) }] }, { "@type": "FAQPage", mainEntity: article.faqs.map((faq) => ({ "@type": "Question", name: faq.question, acceptedAnswer: { "@type": "Answer", text: faq.answer } })) }] };
  return <main className="min-h-screen bg-slate-950 text-white"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: safeJsonLd(schema) }} /><SiteHeader /><article><header className="border-b border-white/10 bg-[#061322]"><div className="mx-auto max-w-5xl px-4 py-14 sm:px-6 sm:py-20 lg:px-8"><Link href="/knowledge" className="inline-flex items-center gap-2 text-sm font-semibold text-sky-300"><ArrowLeft className="h-4 w-4" />Knowledge Hub</Link><div className="mt-7 flex flex-wrap gap-3 text-xs font-semibold uppercase tracking-[0.16em] text-slate-400"><span className="text-sky-300">{article.category}</span><span>·</span><span>{article.readTime}</span><span>·</span><time dateTime={article.updatedAt}>Updated {new Date(`${article.updatedAt}T00:00:00Z`).toLocaleDateString("en-IN", { day: "numeric", month: "long", year: "numeric", timeZone: "UTC" })}</time>{article.refreshCadence ? <><span>·</span><span>{article.refreshCadence} review</span></> : null}</div><h1 className="mt-5 max-w-4xl text-4xl font-semibold tracking-[-0.04em] sm:text-6xl">{article.title}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-slate-300">{article.intro}</p></div></header><div className="mx-auto max-w-5xl space-y-10 px-4 py-14 sm:px-6 lg:px-8"><section className="grid gap-4 rounded-[1.5rem] border border-emerald-300/15 bg-emerald-300/[0.06] p-6 sm:grid-cols-3"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-emerald-200">Reviewed</p><p className="mt-2 font-semibold">{new Date(`${article.updatedAt}T00:00:00Z`).toLocaleDateString("en-IN", { day: "numeric", month: "long", year: "numeric", timeZone: "UTC" })}</p></div><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-emerald-200">Evidence</p><p className="mt-2 font-semibold">{article.sources?.length ? `${article.sources.length} source${article.sources.length === 1 ? "" : "s"} linked` : "Live PlugV catalogue"}</p></div><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-emerald-200">Method</p><Link href="/methodology" className="mt-2 inline-flex font-semibold text-white hover:text-emerald-100">See how PlugV verifies data</Link></div></section>{article.calculator ? <CostCalculator mode={article.calculator} /> : null}{article.slug === "top-electric-cars-by-category-india" ? <CategoryLists /> : null}{article.vehicleList ? <VehicleRankingList mode={article.vehicleList} /> : null}{article.sections.map((section) => <section key={section.heading} className="rounded-[1.5rem] border border-white/10 bg-white/[0.03] p-6 sm:p-8"><h2 className="text-2xl font-semibold text-white">{section.heading}</h2>{section.paragraphs?.map((paragraph) => <p key={paragraph} className="mt-4 text-base leading-8 text-slate-300">{paragraph}</p>)}{section.bullets ? <ul className="mt-5 grid gap-3">{section.bullets.map((bullet) => <li key={bullet} className="flex gap-3 text-base leading-7 text-slate-300"><span className="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-sky-300" />{bullet}</li>)}</ul> : null}</section>)}{article.sources?.length ? <section className="rounded-[1.5rem] border border-emerald-300/15 bg-emerald-300/[0.06] p-6"><h2 className="text-lg font-semibold">Sources used for this review</h2><p className="mt-2 text-sm leading-6 text-slate-300">Open these links to verify time-sensitive claims at the source. Prices, policies and availability can change after the review date.</p><div className="mt-4 grid gap-3">{article.sources.map((source) => <a key={source.url} href={source.url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 text-sm font-semibold text-emerald-200 hover:text-white">{source.label}<ExternalLink className="h-4 w-4" /></a>)}</div></section> : null}<section aria-labelledby="faq-heading"><h2 id="faq-heading" className="text-3xl font-semibold">Frequently asked questions</h2><div className="mt-5 grid gap-4">{article.faqs.map((faq) => <details key={faq.question} className="rounded-2xl border border-white/10 bg-white/[0.03] p-5"><summary className="cursor-pointer font-semibold text-white">{faq.question}</summary><p className="mt-3 text-sm leading-7 text-slate-300">{faq.answer}</p></details>)}</div></section><RelatedGuides article={article} /><div className="rounded-[2rem] border border-sky-300/20 bg-sky-300/10 p-7"><h2 className="text-2xl font-semibold">Continue your EV decision</h2><p className="mt-3 text-sm leading-7 text-slate-300">Use PlugV’s catalogue, charging search and comparison workspace to apply this guide to current vehicles and journeys.</p><div className="mt-5 flex flex-wrap gap-3"><Link href="/vehicles" className="inline-flex items-center gap-2 rounded-full bg-sky-300 px-5 py-3 text-sm font-semibold text-slate-950">Explore EVs <ArrowRight className="h-4 w-4" /></Link><Link href="/compare" className="rounded-full border border-white/15 px-5 py-3 text-sm font-semibold text-white">Compare EVs</Link><Link href="/charging" className="rounded-full border border-white/15 px-5 py-3 text-sm font-semibold text-white">Find chargers</Link></div></div></div></article><SiteFooter /></main>;
}
